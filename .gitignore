# Cache do Python
__pycache__/
*.py[cod]

# Dados baixados da Anatel (recriados ao rodar; podem ser grandes)
dados_anatel/
*.csv
*.zip

# Log local (de uso pessoal, não faz parte do código)
anatel_smp_log.txt

# Credenciais de e-mail: NUNCA versionar (no GitHub Actions vem do secret)
email_config.json

# OBS: anatel_smp_estado.json é versionado de propósito — é assim que o GitHub
# Actions guarda a última competência vista entre execuções (state persistence).

# Ambientes virtuais
.venv/
venv/
env/

# Configs de editor/IDE
.vscode/
.idea/
